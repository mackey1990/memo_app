<h1>メモ編集画面</h1>

<%= form_for @memo, url: "/memos/#{@memo.id}", html: { method: :patch } do |f| %>

  <p><%= f.label :title %></p>
  <p><%= f.text_field :title %></p>

  <p><%= f.label :content %></p>
  <p><%= f.text_area :content %></p>

  <%= f.submit %>
<% end %>
